
const createTestString = (input, sample, stepInfo) => {
   /**

const test = [
  {
    input: [1, 2],
    expected_output: 3,
    sample: true,
  },
  {
    input: [10, 2],
    expected_output: 12,
    sample: true
  }
];

const function_name = 'sum';
test.map((test) => {
  let inputString = '';
  test.input.map((input) => {
    inputString += `${input}, `;
  });

  const testString = `
  test('${inputString}', (t) => {
    t.is(${function_name}(${inputString}), ${test.expected_output});
  });
  `;
});
    */
};

export default createTestString;
