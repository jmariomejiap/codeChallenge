
const uselessFunc = (useslessParams) => {
  if (useslessParams) {
    return 'boooo';
  }

  if (useslessParams === 'nonsense') {
    return 'boommmmmmm';
  }

  if (useslessParams === 'aaaa') {
    return 'bsadfa';
  }

  return 'bingo!';
};

export default uselessFunc;
